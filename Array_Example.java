public class Array_Example {
    public static void main(String[] args) {
        // Create an array with 3 numbers
        int[] numbers = new int[3];

        // assign values to the 3 numbers:
        numbers[0] = 4;
        numbers[1] = -2;
        numbers[2] = 0;

        // print out the 3 numbers:
        for (int i = 0; i < numbers.length; i++) {
            system.out.println("The number at position 0 is" + numbers[i]);
        }

    }}

    // Declare and initialize
    int[] newArrayVariables = { 5, 3, 3, 2 };

    // Calls the array variable
    newArrayVariable[0];

    // How to search an for an element in a array given a sampleArray = ?. We have
    // been given a target to get 2;
    for(int i= 0,i<sampleArray.length;i++)
    {
        if (sampleArray[i] == target)
            return sampleArray[i];
    }

    // Get a class
    public class Averages {
    public static void main(String[]args){

        // Method for averages
        public static double computeAverage(int[] numbers) {
            int sumofNumbers = 0;
            // a -> Get the ssum of the elements in the parameter
            for (int eachNumber : numbers)
                sumofNumbers += eachNumber;

            // return a/b as a double right?
            return sumofNumbers / (double) numbers.length;

        }
    }

}

public static